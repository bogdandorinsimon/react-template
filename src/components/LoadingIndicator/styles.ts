import { SxProps } from "@mui/material";

export const sxStyles = (): { [_: string]: SxProps } => ({
  container: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    width: "100%",
    height: "100%"
  }
});
