export const ROUTER_PATH = {
  PRODUCTS: "/",
  NOT_FOUND: "*"
};

export const BACK_ROUTE = -1;
export const DEFAULT_ROUTE = ROUTER_PATH.PRODUCTS;

export const NAME_AND_EMAIL_MAX_LENGTH = 50;
export const NAME_REGEX = /^[A-Za-z][a-zA-Z'-]*$/;
